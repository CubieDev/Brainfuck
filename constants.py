Statement = str
# We can use a dict to easily represent our Array, 
# as we can then use negative and very large pointers, just like in our rules.
# In practice we use a defaultdict(0).
Array = dict
Pointer = int
Input = str
Output = str